sonar.projectKey=nregati_btree
sonar.organization=nregati


# Required metadata
sonar.projectName=btree
sonar.projectVersion=1.0

# Path to source directories (adjust as needed)
sonar.sources=src

# Encoding of the source code
sonar.sourceEncoding=UTF-8

# Java binaries (if using Maven/Gradle, this is usually set automatically)
sonar.java.binaries=target/classes

# Optional: Exclude test files if needed
# sonar.exclusions=**/test/**

# Optional: Coverage report path (if using JaCoCo)
# sonar.java.coveragePlugin=jacoco
# sonar.jacoco.reportPaths=target/jacoco.exec
